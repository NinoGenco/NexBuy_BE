spring:
  datasource:
    url: jdbc:mysql://localhost:3306/nexbuy_db?createDatabaseIfNotExist=true
    username: kevin
    password: kevin12345.
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect
    open-in-view: false
    defer-datasource-initialization: true